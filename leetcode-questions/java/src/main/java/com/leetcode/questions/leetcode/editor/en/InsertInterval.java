package com.leetcode.questions.leetcode.editor.en;

/**
 * 57. Insert Interval
 * 
 * You are given an array of non-overlapping intervals intervals where intervals[i] = [starti, endi] represent the start and the end of the ith interval and intervals is sorted in ascending order by starti. You are also given an interval newInterval = [start, end] that represents the start and end of another interval.
Insert newInterval into intervals such that intervals is still sorted in ascending order by starti and intervals still does not have any overlapping intervals (merge overlapping inter...
 */
public class InsertInterval {
    //leetcode submit region begin(Prohibit modification and deletion)
public static class Solution {
    public int[][] insert(int[][] intervals, int[] newInterval) { return null; }
}
    //leetcode submit region end(Prohibit modification and deletion)
}
